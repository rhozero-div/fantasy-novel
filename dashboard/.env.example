# PROJECTS_DIR — comma-separated list of directories to scan for projects.
# Each subdirectory containing skill_context/ is shown as a switchable project.
# Example: PROJECTS_DIR=/Users/me/Hermes/projects,/Users/me/workspace
#
# When unset, only the default demo-project is available.
